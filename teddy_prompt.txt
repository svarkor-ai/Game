You are Teddy, the code author. Fix the black screen issue in Djurspel by setting the missing uView and uProjection shader uniforms.

CONTEXT:
- The game renders a completely black screen because shader uniforms are missing
- Shaders use `uView` and `uProjection` matrices but they're never set
- Only `uModel` and `uColor` are set in DrawTileMap/DrawEntity

WHAT YOU NEED TO DO:

1. Add methods to IsometricCamera.cs to return view/projection matrices:
   - `Matrix4 GetViewMatrix()` — for isometric view, use a static diagonal matrix (diagonal(1,1,1)) * translation by -Position
   - `Matrix4 GetProjectionMatrix()` — for isometric view, return identity matrix

2. In Renderer.cs, modify DrawTileMap and DrawEntity to set uView and uProjection:
   - Call `_camera.GetViewMatrix()` and `_camera.GetProjectionMatrix()` 
   - Use `SetUniformMat4` to set both uniforms before `uModel`

3. Use the existing IsometricCamera.Position for the view matrix
4. For a simple isometric view, identity projection works fine

KEY CONSTRAINTS:
- Do NOT change the shader source code
- Do NOT add new dependencies
- Keep the existing code style
- Only modify these files: IsometricCamera.cs, Renderer.cs

When done, END your reply with exactly this block and nothing after it:
--- RESULT ---
STATUS: DONE | BLOCKED
FILES: <paths you created/changed, one per line, or "none">
DID: <what you did, <=4 lines>
VERIFY: <the check you ran + its outcome, or "not run">
BLOCKERS: <concrete blocker, or "none">
--- END ---
